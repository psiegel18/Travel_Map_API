{
  "name": "travel-map-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker that generates interactive travel maps",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@sentry/cloudflare": "^9.32.0"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
